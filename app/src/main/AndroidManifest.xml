<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Basesdedatoslocalesk"
        tools:targetApi="31">
        <activity
            android:name=".LoginUsuarios"
            android:exported="false" />
        <activity
            android:name=".RegistroUsuarios"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.EditarParticipante"
            android:exported="false" />
        <activity
            android:name=".controller.ItemParticipante"
            android:exported="false" />
        <activity
            android:name=".controller.ListaParticipantes"
            android:exported="false" />
        <activity
            android:name=".controller.Registro_participantes"
            android:exported="true" />
        <activity
            android:name=".controller.MainActivity"
            android:exported="false" />
    </application>

</manifest>